[package]
name = "physics"
version = "0.1.0"
edition = "2021"
workspace = "NO_PATH"

[lib]
crate-type = ["cdylib"]
path = "src/project.rs"

[dependencies]
pill_engine = { path = "../../engine/pill_engine", features = [
    "project",
    "physics",
    "debug_ui",
] }

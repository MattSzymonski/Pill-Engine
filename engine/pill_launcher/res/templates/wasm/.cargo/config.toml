# Required by getrandom >=0.3 on wasm32-unknown-unknown.
# See: https://docs.rs/getrandom/0.3/#webassembly-support
[target.wasm32-unknown-unknown]
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]
